// Keeps a stable source anchor for thumbnail generation progress-window integration so file references remain predictable across regeneration.

namespace NoodleHammer.PreviewForge.Editor
{
    // Intentionally left as an anchor file to keep project file references stable
    // across Unity/IDE regeneration cycles. Implementation lives in ParticleThumbnailService.cs.
    internal static class ParticleThumbnailGenerateProgressWindowFileAnchor
    {
    }
}
